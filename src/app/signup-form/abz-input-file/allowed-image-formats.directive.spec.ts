import { AllowedImageFormatsDirective } from './allowed-image-formats.directive';

describe('AllowedImageFormatsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedImageFormatsDirective();
    expect(directive).toBeTruthy();
  });
});
