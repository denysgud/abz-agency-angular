import { MaxFileSizeDirective } from './max-file-size.directive';

describe('MaxFileSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxFileSizeDirective();
    expect(directive).toBeTruthy();
  });
});
