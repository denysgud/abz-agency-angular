import { MinImageResolutionDirective } from './min-image-resolution.directive';

describe('MinImageResolutionDirective', () => {
  it('should create an instance', () => {
    const directive = new MinImageResolutionDirective();
    expect(directive).toBeTruthy();
  });
});
